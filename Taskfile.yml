# https://taskfile.dev

version: '3'

includes:
  tf: ./taskfiles/terraform.yml
  releases: ./taskfiles/helmfile.yml

vars:
  GREETING: Hello, World!

tasks:
  default:
    cmds:
      - echo "{{.GREETING}}"
    silent: true
